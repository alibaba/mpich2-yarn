# This Makefile.in created by simplemake.  Do not edit

# @configure_input@

# --------------------------------------------------------------------------
all: all-redirect

SHELL           = @SHELL@
abs_builddir    = @abs_builddir@
srcdir          = @srcdir@
abs_srcdir      = @abs_srcdir@
MAKE            = @MAKE@
MPILIBNAME      = @MPILIBNAME@
PMPILIBNAME     = @PMPILIBNAME@


VPATH = .:@srcdir@
HEADERS = mpi.h mpiimpl.h mpiatomic.h mpichtimer.h mpierror.h mpierrs.h \
	mpihandlemem.h mpimem.h mpistates.h mpisysstates.h mpitimerimpl.h \
	mpitypedefs.h mpiutil.h mpiallstates.h mpiimplthread.h \
	mpiiov.h mpiu_monitors.h mpichtimer.h mpich_param_vals.h

.SUFFIXES:
# Some make programs complain if no suffixes are set
.SUFFIXES: .c .o
apply: 
	$(ACTION) $(ACTION_INPUT)
clean: 
	-@rm -f *.o ${srcdir}/*.o 
	-@rm -f *.lo ${srcdir}/*.lo
distclean:  clean distclean-xxx remove-makefile
distclean-xxx:
	-@rm -f Makefile mpich2info.h mpichtimer.h mpi.h
	-@rm -rf autom4te*.cache
	-@rm -rf $(DEPS_DIR)
	-@rm -f TAGS
remove-makefile:
	rm -f Makefile
remove-genmakefiles:
	rm -f  Makefile
all-redirect:

# --------------------------------------------------------------------------
# --------------------------------------------------------------------------
# --------------------------------------------------------------------------
HEADERFILES     = $(abs_builddir)/mpi.h mpiimpl.h mpiatomic.h $(abs_builddir)/mpichtimer.h \
	mpierror.h mpierrs.h mpihandlemem.h mpimem.h mpistates.h mpisysstates.h mpitimerimpl.h \
	mpitypedefs.h mpiutil.h mpiallstates.h mpiimplthread.h mpiiov.h mpiu_monitors.h \
	$(abs_builddir)/mpichtimer.h mpich_param_vals.h 
SOURCEFILES     = $(SOURCES)
# --------------------------------------------------------------------------
# This Makefile supports the TAGS target

.POSIX:

FORCE_TARGET:

